# BlogBot Requirements — Python 3.11 PINNED
# Generated: Phase 0 | Never auto-upgrade — pin versions are deliberate
# Install: pip install -r requirements.txt

# ── Browser Automation & Session Extraction ──────────────────────────────────
playwright==1.41.2
browser-cookie3==0.17.1
webdriver-manager==4.0.1
selenium==4.17.2

# ── HTTP & Networking ────────────────────────────────────────────────────────
requests==2.31.0
httpx==0.26.0
urllib3==2.1.0

# ── Database ─────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.25

# ── Encryption & Security ────────────────────────────────────────────────────
cryptography==42.0.2
bcrypt==4.1.2

# ── AI Content Generation APIs ───────────────────────────────────────────────
groq==0.4.2
google-generativeai==0.3.2
cohere==4.47
mistralai==0.1.8

# ── Translation ──────────────────────────────────────────────────────────────
deepl==1.16.1
langdetect==1.0.9

# ── Scheduling ───────────────────────────────────────────────────────────────
APScheduler==3.10.4

# ── Image Processing ─────────────────────────────────────────────────────────
Pillow==10.2.0

# ── HTML / RSS / Feed Parsing ─────────────────────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.1.0
feedparser==6.0.11

# ── Dashboard (PyQt5 — native Windows) ───────────────────────────────────────
PyQt5==5.15.10
PyQt5-Qt5==5.15.12
PyQt5-sip==12.13.0

# ── System Monitoring ────────────────────────────────────────────────────────
psutil==5.9.8

# ── Time / NTP ───────────────────────────────────────────────────────────────
ntplib==0.4.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
PyYAML==6.0.1
python-dateutil==2.8.2
pytz==2024.1
tqdm==4.66.2
tenacity==8.2.3
pydantic==2.6.1
aiohttp==3.9.3

# ── Google APIs ──────────────────────────────────────────────────────────────
google-api-python-client==2.116.0
google-auth==2.27.0
google-auth-httplib2==0.2.0

# ── Email ────────────────────────────────────────────────────────────────────
sib-api-v3-sdk==7.6.0

# ── Content Scraping / Trend Detection ───────────────────────────────────────
pytrends==4.9.2
tweepy==4.14.0

# ── Encoding / Text ──────────────────────────────────────────────────────────
chardet==5.2.0
ftfy==6.1.3

# ── SESSION-040 Library Integrations (2026-04-25) ────────────────────────────
# All packages from PyPI only. Pinned versions. Zero runtime code downloads.
# Security audit: each package's network endpoints documented in BUILD_LOG.md.

# Text quality (zero network calls — pure local text analysis)
textstat==0.7.3        # Flesch readability scoring in quality_control.py
clean-text==0.6.0      # Unicode normalization, encoding fixes in quality_control.py

# GitHub API wrapper (connects ONLY to api.github.com)
PyGithub==2.3.0        # Optional rate-limit helper in github_publisher.py

# Cloudflare API SDK — SKIPPED: cloudflare==3.1.0 is incompatible with Python 3.14
# (Pydantic v1 compat layer breaks on 3.14 — TraceItem annotation error at import)
# cloudflare_manager.py uses raw requests instead (working correctly)
# Re-evaluate when cloudflare SDK publishes Python 3.14-compatible release

# Social media publishers (connect only to their respective official APIs)
pytumblr==0.1.0        # Tumblr link posts → api.tumblr.com (tumblr_publisher.py)
# tweepy==4.14.0 already present above — Twitter API v2 → api.twitter.com

# Unified notifications (connects only to services YOU configure)
apprise==1.8.0         # 70+ notification services via single API (alert_system.py)
